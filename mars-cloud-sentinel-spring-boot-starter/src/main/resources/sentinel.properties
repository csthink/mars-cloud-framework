# Sentinel 在类初始化时从 classpath 上的全部 sentinel.properties 读取配置，JVM 系统属性覆盖其上。
# 关闭每秒写一次的指标文件：它只供 Dashboard 读取，而本组件不装 Dashboard。放在这里，
# 不论哪段代码先触发 Sentinel 初始化（包括不经 Spring 启动的代码），读到的都是 0。
csp.sentinel.metric.flush.interval=0
