<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- 直接继承公司 BOM，统一版本/仓库/发布配置 -->
    <parent>
        <groupId>com.mars.cloud</groupId>
        <artifactId>mars-cloud-dependencies</artifactId>
        <version>1.0.0-SNAPSHOT</version> <!-- 建议尽快使用 Release 版本 -->
        <relativePath>../mars-cloud-dependencies/pom.xml</relativePath>
    </parent>

    <artifactId>mars-cloud-mysql</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>mars-cloud-mysql</name>
    <description>MySQL 相关处理</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.release>${java.version}</maven.compiler.release>
    </properties>

    <dependencies>
        <!-- 依赖纯工具/模型库（由 BOM 管版本，不写 <version>） -->
        <dependency>
            <groupId>com.mars.cloud</groupId>
            <artifactId>mars-cloud-common</artifactId>
        </dependency>

        <dependency>
            <groupId>com.mars.cloud</groupId>
            <artifactId>mars-cloud-core-spring-boot-starter</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- 自动装配核心 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-autoconfigure</artifactId>
        </dependency>

        <!-- 生成 spring-configuration-metadata.json，提升应用侧配置提示体验 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-configuration-processor</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!--
          MyBatis-Plus 的 Boot 4 专用 starter。mybatis-spring-boot-starter 由它传递引入，
          不再单独声明——单独声明会把 MyBatis 的版本决定权从 MyBatis-Plus 手里拿走。
        -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot4-starter</artifactId>
        </dependency>

        <!-- 分页拦截器（3.5.9 起不在 starter 内，需显式引入） -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-jsqlparser</artifactId>
        </dependency>

        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>transmittable-thread-local</artifactId>
        </dependency>

    </dependencies>

    <!-- 发布仓库配置由 parent 继承，这里不再重复 -->

    <build>
        <!-- 对于库，通常不自定义 finalName；让 Maven 默认产物命名即可 -->
        <plugins>
            <!-- 标准编译（使用 release 语义），并配置注解处理器 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <encoding>UTF-8</encoding>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.springframework.boot</groupId>
                            <artifactId>spring-boot-configuration-processor</artifactId>
                            <version>${spring-boot.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- 附带源码与文档，便于下游复用与代码浏览 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>3.3.1</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals><goal>jar</goal></goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.11.2</version>
                <configuration>
                    <source>${java.version}</source>
                    <encoding>UTF-8</encoding>
                    <doclint>none</doclint>
                    <!-- 如果希望遇到缺失也不中断，可以临时加上： -->
                    <!-- <failOnError>false</failOnError> -->
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>com.mars.cloud</groupId>
                        <artifactId>mars-cloud-core-spring-boot-starter</artifactId>
                        <version>${project.version}</version>
                        <!-- 这里不要写 scope，让它仅作为“插件依赖”存在 -->
                    </dependency>
                </dependencies>
            </plugin>

            <!-- 可选：给模块起一个稳定的 JPMS 名称 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.5.1</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Automatic-Module-Name>com.mars.cloud.mysql</Automatic-Module-Name>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>

            <!-- 可选：一致性约束 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>enforce</id>
                        <goals><goal>enforce</goal></goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[17,)</version>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
